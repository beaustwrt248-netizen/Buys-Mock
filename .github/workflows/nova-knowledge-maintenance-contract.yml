name: Nova Knowledge Maintenance Contract

on:
  pull_request:
    branches: [ main ]
    paths:
      - 'supabase/functions/nova-knowledge-maintenance/**'
      - 'supabase/migrations/*nova_knowledge_maintenance_pipeline.sql'
      - 'tests/nova_knowledge_maintenance_contract.test.mjs'
      - '.github/workflows/nova-knowledge-maintenance-contract.yml'
  push:
    branches: [ main ]
    paths:
      - 'supabase/functions/nova-knowledge-maintenance/**'
      - 'supabase/migrations/*nova_knowledge_maintenance_pipeline.sql'
      - 'tests/nova_knowledge_maintenance_contract.test.mjs'
      - '.github/workflows/nova-knowledge-maintenance-contract.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  contract:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Run Nova knowledge maintenance contract
        run: node --test tests/nova_knowledge_maintenance_contract.test.mjs
