name: Private Distribution Readiness

on:
  pull_request:
    branches: [ main ]
    paths:
      - 'admin/**'
      - 'android/adminapp/**'
      - 'android/settings.gradle'
      - 'ota/latest.json'
      - 'scripts/build_private_distribution_readiness.py'
      - '.github/workflows/admin-apk-build.yml'
      - '.github/workflows/private-distribution-readiness.yml'
      - 'docs/security/private-repository-migration-readiness.md'
  push:
    branches: [ main ]
    paths:
      - 'admin/**'
      - 'android/adminapp/**'
      - 'android/settings.gradle'
      - 'ota/latest.json'
      - 'scripts/build_private_distribution_readiness.py'
      - '.github/workflows/admin-apk-build.yml'
      - '.github/workflows/private-distribution-readiness.yml'
      - 'docs/security/private-repository-migration-readiness.md'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-readiness-bundle:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false
      - name: Build portable Admin/OTA readiness bundle
        run: python3 scripts/build_private_distribution_readiness.py
      - name: Verify bundle checksums
        working-directory: dist/private-distribution-readiness
        run: sha256sum --check SHA256SUMS
      - name: Assert Admin Android distribution is private-repo compatible
        shell: bash
        run: |
          set -euo pipefail
          grep -q 'permissions:' .github/workflows/admin-apk-build.yml
          grep -q 'contents: read' .github/workflows/admin-apk-build.yml
          grep -q 'persist-credentials: false' .github/workflows/admin-apk-build.yml
          grep -q 'sha256sum --check dist/Morley-Admin-readonly.apk.sha256' .github/workflows/admin-apk-build.yml
          grep -q 'actions/upload-artifact@' .github/workflows/admin-apk-build.yml
          if grep -Eq 'raw\.githubusercontent\.com|github\.io|releases/download' .github/workflows/admin-apk-build.yml; then
            echo 'Admin APK build must not depend on public repository distribution URLs.' >&2
            exit 1
          fi
      - name: Assert live distribution is untouched
        shell: bash
        run: |
          set -euo pipefail
          grep -q 'raw.githubusercontent.com/beaustwrt248-netizen/Buys-Mock/main/ota/latest.json' admin/release-control.js
          python3 - <<'PY'
          import json
          from pathlib import Path
          data=json.loads(Path('ota/latest.json').read_text(encoding='utf-8'))
          assert str(data['apkUrl']).startswith('https://github.com/beaustwrt248-netizen/Buys-Mock/releases/download/')
          PY
      - name: Upload migration-readiness bundle
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: morley-private-distribution-readiness
          path: dist/private-distribution-readiness
          if-no-files-found: error
          retention-days: 14
