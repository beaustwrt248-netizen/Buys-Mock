(()=>{
'use strict';
const C={bg:'#080b0d',surface:'#101619',raised:'#151d20',accent:'#38d6a3',accentSoft:'#173c32',text:'#f4f7f6',secondary:'#b2c0bc',muted:'#81918c',border:'#2b4540',good:'#63e6a6',warn:'#f5c76b',bad:'#ff7b86'};
function style(){let s=document.getElementById('guardianUltimateStyle');if(!s){s=document.createElement('style');s.id='guardianUltimateStyle';document.head.appendChild(s)}s.textContent=`body{background:${C.bg}!important;color:${C.text}!important}.card{background:linear-gradient(180deg,${C.raised},${C.surface})!important;border-color:${C.border}!important}.eyebrow,.status.ok{color:${C.accent}!important}button:not(.danger),.ghost{border-color:${C.border}!important}.guardian-health-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(155px,1fr));gap:10px;margin-top:12px}.guardian-health-tile{padding:14px;border:1px solid ${C.border};border-radius:16px;background:${C.bg}}.guardian-health-tile span{display:block;color:${C.muted};font-size:11px;font-weight:800;text-transform:uppercase;letter-spacing:.06em}.guardian-health-tile strong{display:block;margin-top:6px;color:${C.text};font-size:18px}.guardian-health-note{margin-top:12px;padding:12px 14px;border:1px solid ${C.border};border-radius:14px;color:${C.secondary};line-height:1.5}.guardian-health-good{color:${C.good}!important}.guardian-health-warn{color:${C.warn}!important}.guardian-health-bad{color:${C.bad}!important}`}
function shell(){if(document.getElementById('guardianHealthPanel'))return;const anchor=document.querySelector('.grid');if(!anchor)return;const p=document.createElement('section');p.className='card';p.id='guardianHealthPanel';p.innerHTML='<h2>Guardian health & safety posture</h2><p class="muted">Live control-state and incident-freshness checks. Guardian remains human-controlled for code, deployment, authentication, RLS, secrets and destructive database actions.</p><div class="guardian-health-grid"><div class="guardian-health-tile"><span>Control posture</span><strong id="ghPosture">Checking…</strong></div><div class="guardian-health-tile"><span>Worker freshness</span><strong id="ghFreshness">Checking…</strong></div><div class="guardian-health-tile"><span>Stale open incidents</span><strong id="ghStale">—</strong></div><div class="guardian-health-tile"><span>Safety boundary</span><strong class="guardian-health-good">Human protected</strong></div></div><div id="ghNote" class="guardian-health-note">Running Guardian health checks…</div>';anchor.insertAdjacentElement('afterend',p)}
function ageLabel(date){if(!date)return 'No activity';const ms=Date.now()-new Date(date).getTime();if(!Number.isFinite(ms))return 'Unknown';const m=Math.max(0,Math.round(ms/60000));if(m<2)return 'Just now';if(m<60)return `${m} min ago`;const h=Math.round(m/60);return h<48?`${h} hr ago`:`${Math.round(h/24)} d ago`}
function cls(el,name){el.classList.remove('guardian-health-good','guardian-health-warn','guardian-health-bad');el.classList.add(name)}
async function load(){shell();if(!window.sb)return;const posture=document.getElementById('ghPosture'),fresh=document.getElementById('ghFreshness'),stale=document.getElementById('ghStale'),note=document.getElementById('ghNote');if(!posture)return;try{const [{data:settings,error:se},{data:incidents,error:ie}]=await Promise.all([window.sb.from('guardian_settings').select('*').limit(1).maybeSingle(),window.sb.from('guardian_incidents').select('id,status,updated_at,created_at,risk_level,requires_approval').order('updated_at',{ascending:false}).limit(100)]);if(se)throw se;if(ie)throw ie;const kill=!!settings?.kill_switch,enabled=settings?.enabled!==false,auto=!!settings?.auto_fix_enabled,mode=settings?.mode||settings?.operating_mode||'observe';posture.textContent=kill?'KILL SWITCH':(!enabled?'PAUSED':`${String(mode).replace('_',' ').toUpperCase()}${auto?' • AUTO':''}`);cls(posture,kill?'guardian-health-bad':(!enabled?'guardian-health-warn':'guardian-health-good'));const latest=(incidents||[])[0]?.updated_at||(incidents||[])[0]?.created_at;fresh.textContent=ageLabel(latest);const latestAge=latest?Date.now()-new Date(latest).getTime():Infinity;cls(fresh,latestAge<6*3600000?'guardian-health-good':latestAge<24*3600000?'guardian-health-warn':'guardian-health-bad');const cutoff=Date.now()-24*3600000;const open=(incidents||[]).filter(i=>!['resolved','closed','rejected'].includes(String(i.status||'').toLowerCase()));const staleOpen=open.filter(i=>new Date(i.updated_at||i.created_at||0).getTime()<cutoff);stale.textContent=String(staleOpen.length);cls(stale,staleOpen.length===0?'guardian-health-good':staleOpen.length<3?'guardian-health-warn':'guardian-health-bad');const approval=open.filter(i=>i.requires_approval).length;note.textContent=kill?'Guardian intake and automatic repair are stopped by the emergency kill switch.':staleOpen.length?`${staleOpen.length} open incident${staleOpen.length===1?' is':'s are'} older than 24 hours; ${approval} open incident${approval===1?' requires':'s require'} human approval.`:`Guardian controls are responsive. ${open.length} incident${open.length===1?' is':'s are'} currently open; ${approval} require human approval.`}catch(e){posture.textContent='CHECK FAILED';cls(posture,'guardian-health-bad');fresh.textContent='Unavailable';stale.textContent='—';note.textContent=`Health telemetry could not load: ${e?.message||e}. Existing Guardian safety controls remain unchanged.`}}
style();shell();window.addEventListener('load',load,{once:true});document.getElementById('guardianRefreshBtn')?.addEventListener('click',()=>setTimeout(load,350));setInterval(load,60000);setTimeout(load,400);
})();
