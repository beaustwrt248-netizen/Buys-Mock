(()=>{'use strict';
function ingest(input){const source=window.MorleyMarketplaceSource,runtime=window.MorleyIntelligenceRuntime;if(!source?.snapshot||!runtime?.ingest)return {state:'unavailable',reason:'Marketplace source/runtime unavailable'};const snapshot=source.snapshot(input);if(snapshot.state!=='confirmed'||!snapshot.authoritativeStock||!snapshot.authoritativeListings){runtime.clear?.('marketplace');return {state:'unavailable',reason:'Both authoritative Cash Converters stock and Gumtree listing evidence are required',checkedAt:snapshot.checkedAt}}runtime.ingest('marketplace',[{stock:snapshot.stock,listings:snapshot.listings}],{source:snapshot.source,checkedAt:snapshot.checkedAt,authoritative:true});return {state:'confirmed',stockCount:snapshot.stock.length,listingCount:snapshot.listings.length,checkedAt:snapshot.checkedAt}}
window.MorleyMarketplaceReconciliation=Object.freeze({ingest});window.addEventListener('morley:authoritative-marketplace-snapshot',event=>ingest(event.detail||{}));
})();