(()=>{'use strict';const $=(s,r=document)=>r.querySelector(s),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));let severity='all',last={state:'unavailable',findings:[],counts:{high:0,medium:0,low:0}};
async function load(){const status=$('#guardianAnalystStatus');if(status)status.textContent='Refreshing Guardian analysis…';if(!window.sb||!window.MorleyGuardianAnalystModel){if(status)status.textContent='Guardian analyst is waiting for authenticated evidence access.';return}const [ir,ar]=await Promise.all([window.sb.from('guardian_incidents').select('id,source,state,risk_level,classification,confidence,last_error_code,worker_version,attempt_count,created_at,updated_at').order('created_at',{ascending:false}).limit(150),window.sb.from('admin_audit_log').select('action,target_type,target_id,details,created_at').order('created_at',{ascending:false}).limit(100)]);if(ir.error){if(status)status.textContent=`Guardian analysis unavailable: ${ir.error.message}`;return}last=window.MorleyGuardianAnalystModel.analyse(ir.data||[],ar.error?[]:(ar.data||[]));render();if(status)status.textContent=ar.error?'Incident analysis refreshed; audit evidence was unavailable.':'Guardian incident and audit evidence refreshed.'}
function render(){let root=$('#guardianAnalyst');if(!root){const metrics=$('.guardian-metric-grid');if(!metrics)return;root=document.createElement('section');root.id='guardianAnalyst';root.className='guardian-analyst-card';metrics.insertAdjacentElement('afterend',root)}const findings=window.MorleyGuardianAnalystModel?.filter(last,severity)||[];root.innerHTML=`<div class="gaa-head"><div><small>GUARDIAN ANALYST</small><h2>Failure & risk analysis</h2><p>Explains incident patterns and review priorities from Guardian evidence. It cannot approve, retry or apply repairs.</p></div><span class="gaa-state" data-state="${esc(last.state)}">${last.state==='unavailable'?'EVIDENCE N/A':esc(last.state.toUpperCase())}</span></div><div class="gaa-metrics"><span><b>${last.counts.high}</b>High</span><span><b>${last.counts.medium}</b>Medium</span><span><b>${last.counts.low}</b>Low</span></div><div class="gaa-tools"><select id="guardianAnalystSeverity" aria-label="Filter Guardian analyst findings"><option value="all">All findings</option><option value="high">High</option><option value="medium">Medium</option><option value="low">Low</option></select><button id="guardianAnalystRefresh" class="ghost" type="button">Refresh analysis</button></div><div id="guardianAnalystStatus" class="status" role="status" aria-live="polite"></div><div class="gaa-list" aria-live="polite">${last.state==='unavailable'?'<div class="gaa-empty" role="status"><b>Guardian evidence unavailable</b><span>No incident evidence has been loaded for analysis.</span></div>':findings.length?findings.map(f=>`<article class="gaa-row"><div class="gaa-rowhead"><b>${esc(f.title)}</b><span>${esc(f.severity.toUpperCase())}</span></div><p>${esc(f.detail)}</p><em>${esc(f.recommendation)}</em></article>`).join(''):'<div class="gaa-empty" role="status"><b>No findings in this filter</b><span>Available Guardian evidence does not show a matching analyst finding.</span></div>'}</div>`;$('#guardianAnalystSeverity',root).value=severity;$('#guardianAnalystSeverity',root).onchange=e=>{severity=e.target.value;render()};$('#guardianAnalystRefresh',root).onclick=()=>load().catch(()=>{})}
function boot(){render();let tries=0;const timer=setInterval(()=>{if(window.sb){clearInterval(timer);load().catch(()=>{})}else if(++tries>20)clearInterval(timer)},300)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot()})();
