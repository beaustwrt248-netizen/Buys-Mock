# Nova Next adapters

Adapters are the only browser-facing boundary to approved backend services. Keep them narrow, authenticated, evidence-aware and fail-closed. Do not import existing `nova/` frontend/runtime modules into this directory.
