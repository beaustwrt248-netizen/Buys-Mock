Guardian repair candidate generation is dispatched server-side from Postgres using the existing Guardian incident dispatch nonce. The Edge Function gateway leaves JWT verification disabled only so database dispatch and browser preflight can reach the function; the function itself requires either an authenticated Admin/Manager session or the matching incident dispatch nonce before processing a requested repair.
