import test from 'node:test';import assert from 'node:assert/strict';import {createRequire} from 'node:module';const require=createRequire(import.meta.url);const business=require('../admin/business-intelligence-model.js');
test('business analytics excludes CRM and inventory lifecycle metrics',()=>{const out=business.summarise({catalogue:{state:'confirmed',devices:[{id:1}],findings:[]},inventory:[{id:'stock-1'}],customers:[{id:'customer-1'}]});const labels=out.metrics.map(x=>x.label.toLowerCase()).join(' ');assert.doesNotMatch(labels,/customer|crm|stocktake|shelf|bin|inventory lifecycle/);assert.match(out.note,/No CRM or inventory-lifecycle analytics/);});