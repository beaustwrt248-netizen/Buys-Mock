import test from 'node:test';import assert from 'node:assert/strict';import {readFile} from 'node:fs/promises';
test('Admin loads device testing and business intelligence surfaces',async()=>{const src=await readFile(new URL('../admin/admin-home.js',import.meta.url),'utf8');assert.match(src,/device-testing-centre\.js\?v=1/);assert.match(src,/business-intelligence-model\.js\?v=1/);assert.match(src,/business-intelligence-centre\.js\?v=1/);});
test('Command Centre boots only once',async()=>{const src=await readFile(new URL('../admin/intelligence-command-centre.js',import.meta.url),'utf8');assert.match(src,/let booted=false/);assert.match(src,/if\(booted\|\|!appReady\(\)\)return;booted=true/);});