# Refresh behaviour

Every Recovery Centre status refresh fetches current backup metadata and reconstructs the display model, so both newly created and existing backups receive local formatting without altering the backend records.
