# Android compatibility

Morley Android already targets minSdk 26, where `java.time.OffsetDateTime`, `ZoneId` and `DateTimeFormatter` are available. No new third-party dependency or desugaring configuration is introduced by this formatter.
