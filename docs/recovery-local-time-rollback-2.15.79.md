# Rollback boundary

If the timestamp presentation regresses, rollback is limited to the Android display formatter/version release. Existing encrypted backups and backend records remain valid because this patch does not mutate stored timestamps or backup payloads.
