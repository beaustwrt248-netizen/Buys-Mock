# Google Drive contract unchanged

The app continues to request only the existing `drive.appdata` scope for this feature. Account connection, token acquisition and private app-data storage are unaffected by the timestamp presentation change.
