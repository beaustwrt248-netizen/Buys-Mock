# Release summary

Morley Android 2.15.79 makes encrypted Google Drive backup dates human-readable by converting backend offset timestamps to the device's local date/time format. It includes deterministic regression coverage and preserves all existing backup security and restore behaviour.
