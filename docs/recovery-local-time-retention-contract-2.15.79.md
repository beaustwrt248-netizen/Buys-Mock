# Retention contract unchanged

The backend remains responsible for retaining up to 30 encrypted versions. The Android formatter neither reorders backup records nor changes retention decisions; it only changes how each returned `created_at` value is displayed.
