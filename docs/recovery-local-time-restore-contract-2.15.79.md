# Restore contract unchanged

Restore preview, `RESTORE` confirmation, safety backup creation and native workspace import are untouched. Friendly timestamp formatting affects only values presented to the user in the Recovery Centre.
