# Why native formatting

The backend correctly returns absolute timestamps and should remain locale-neutral. Formatting in Android lets each authorized user's device choose its own zone and locale without changing stored backup metadata or web consumers.
