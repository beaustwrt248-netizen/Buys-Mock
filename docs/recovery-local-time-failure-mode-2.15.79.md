# Failure mode

If parsing fails, the UI displays the original timestamp instead of failing the snapshot load. Backup history remains usable, and Verify/Restore/Delete continue to address records by their unchanged backend backup IDs.
