# Final review target

Before merge, confirm the exact latest branch head passes protected `audit` and `security-audit` checks plus the normal Android build/validation suite. After merge, verify the signed release and protected OTA manifest rather than assuming PR success equals deployment.
