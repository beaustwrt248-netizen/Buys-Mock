# Morley Ecosystem Autopilot — 2026-09-13 16:00 AWST

## Reconciled baseline
- Production `main` is `28830b03655e5bb3f7e63b54621fd496ee49faaf`.
- Gumtree remains excluded.
- Guardian remains clear in the fresh production query: 28 incidents, all `resolved`.
- Backup metadata remains unchanged: 2 backups are `ready`; latest creation is 2026-09-12 13:30 UTC. Recovery issue #1785 remains open because metadata alone is not restore evidence.

## Nova maintenance scheduler investigation (#1872)
- Recurring Nova knowledge maintenance remains disabled; no cron restoration was attempted.
- `cron.job_run_details` confirms both the 07:15 and 07:20 attempts executed the same database cron job id 9 successfully at the pg_cron layer.
- `net._http_response` shows request 1831 at 07:15 returned HTTP 200, request 1832 at 07:20 returned HTTP 401, and the immediate direct request 1833 at 07:20:51 returned HTTP 200.
- All three responses came through Supabase Edge region `ap-southeast-1` with different Deno execution IDs, isolating the observed failure to an Edge-worker authorization execution rather than a pg_cron scheduling failure.
- Vault metadata confirms `morley_backup_scheduler_secret` was created and last updated on 2026-09-03; no contemporaneous secret rotation explains the 07:20 failure.
- Production `nova-knowledge-maintenance` remains ACTIVE version 2 with custom scheduler auth (`verify_jwt=false`).

## Regression-first diagnostic PR
- Draft PR #1873 was created from fresh current main.
- RED was verified first: Nova Knowledge Maintenance Contract run 19 failed before implementation because the new scheduler-auth diagnostic contract was absent.
- The worker now classifies authorization outcomes only into bounded non-secret reason codes: `missing_config`, `missing_header`, `env_match`, `rpc_match`, `rpc_error`, `mismatch`.
- Rejection logs include only the reason code; supplied credentials, environment secret material and token/cookie/authorization values are not logged or returned.
- After implementation, an unrelated existing success-response contract initially collided with the helper's earlier `ok: true`; the test was corrected to target the actual final success response rather than weakening the no-secret assertion.
- Exact current PR head: `2d1329895970242090f09a8e5a18d282e35bf853`.
- Current-head Nova Knowledge Maintenance Contract, Repository Security Audit, B&L Morley Quality Gate, Full Feature Contract Audit, Morley Restore Point Capture and Morley Email Contract are green; Ultimate Parity is still running at reconciliation time.
- Because #1873 changes scheduler authentication-path behavior, merge/deploy remains explicit-approval gated even though it does not alter credentials, Vault ownership, RLS, service-role grants or the matcher RPC.

## Protected maintenance hardening
- PR #1859 remains open, stale/conflicted relative to current main, and still describes a 20-embedding schedule that is inconsistent with production cold/scheduled evidence.
- Do not merge/apply/deploy #1859 until it is reconciled with #1867, issue #1872, the verified 4/4 scheduled envelope and current main, then explicitly approved.

## Next actions
1. Let #1873 complete exact-head CI; if fully green, mark it ready as an approval-gated diagnostic patch but do not merge/deploy without explicit approval.
2. After approved diagnostic deployment, reproduce scheduled-path auth repeatedly at 4/4 and use reason codes to isolate missing-header vs matcher/runtime failure before restoring recurring maintenance.
3. Keep #1859 blocked until the diagnostic evidence is incorporated and its schedule/claiming design is rebased safely.
4. Continue non-protected Admin/Morley Buys parity and Nova Next verification lanes while protected Nova maintenance work waits.
