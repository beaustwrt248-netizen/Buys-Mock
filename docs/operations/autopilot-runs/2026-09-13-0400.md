# Morley Autopilot Reconciliation — 2026-09-13 04:00 AWST

This is a non-sensitive append-only run note for the canonical Morley autopilot state. Gumtree remains excluded.

## Repository / CI

- Documentation-only ledger PR #1777 completed all applicable exact-head checks successfully and was squash-merged into `main` as `74590e14df081edb5285c591338f0b42b2b6e906`.
- High-risk Nova/Supabase PR #1764 remains open, unmerged and unapplied pending Beau's explicit approval; its protected guard failures remain intentional and must not be weakened.
- No direct-to-main write or repository-protection bypass was used.

## Production-first triage

- Fresh read-only production query found **0 unresolved Guardian incidents**.
- `nova_ai_runs` still has **0 rows in the last 7 days**, so there is insufficient production telemetry to justify provider/default-routing changes.
- `user_drive_backups` currently has **2 ready rows**; newest ready backup remains `2026-09-12 13:30:07.158513+00`.
- This is backup-freshness evidence only. Full recovery readiness still requires a supported isolated non-destructive restore/read-back drill; production overwrite remains approval-gated.
- Connected Vercel account exposed no teams through the available project-service connector, so no Vercel production deployment state is asserted from this run.

## Nova Next accessibility audit

- Source review of `nova-next/app.js` and `nova-next/index.html` confirmed two accessibility defects in the isolated successor bootstrap:
  1. Drawer close paths can leave focus inside a now-hidden `aria-hidden="true"` drawer because focus is moved into the drawer on open but not restored to the menu trigger on close; there is also no focus containment while open.
  2. Tools/Tasks controls declare `role="tablist"` while child buttons do not implement tab semantics (`role="tab"`, `aria-selected`, keyboard navigation or panel relationships); current behavior only toggles a CSS class.
- Issue #1778 now tracks the evidence and the safe regression-test shape. No runtime/auth/schema/production routing change was made in this run.

## Catalogue integrity

- Issue #1771 remains classification-complete for all 15 active shared brand+model-number groups.
- Apple `A2724` is preserved as a legitimate Apple Watch SE 2 cellular 44mm Europe/Asia-Pacific hardware identifier; the imported `MNQ23ZP/A` retail SKU-to-engraved-A-model mapping remains intentionally unasserted because evidence conflicts.
- Apple `A3461` remains the strongest likely true-duplicate candidate. Prior read-only checks found no pricing, price-history, inventory or audit-finding references; row 1197's three audit-queue rows are historical verified entries, not active blockers. No destructive catalogue action was taken.

## Protected / blocked work

- #1764 remains blocked pending explicit approval because it changes production Supabase schema/function behavior and external-AI data flow.
- #1686 remains blocked pending explicit approval because it changes production Auth security configuration.
- Guardian code-changing repairs, destructive catalogue reconciliation, RLS/schema/index changes, production restore/overwrite, secrets/credentials, signing/OTA promotion and protected pricing authority remain human-approval gated.

## Next safe lanes

1. Address #1778 through a fresh isolated Nova Next branch using regression-first accessibility coverage, without promoting Nova Next or touching production auth.
2. Continue evidence-only catalogue reconciliation for #1771; any A3461 merge/deactivation proposal remains reversible and approval-gated.
3. Continue evidence gathering for Supabase performance issue #1770 without schema/RLS/index mutation.
4. Continue backup/recovery evidence gathering without production overwrite.
