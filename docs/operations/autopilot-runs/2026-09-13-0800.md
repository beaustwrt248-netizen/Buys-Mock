# Morley Autopilot Reconciliation — 2026-09-13 08:00 AWST

This is a non-sensitive append-only run note for the canonical Morley autopilot state. Gumtree remains excluded.

## Repository / active work

- Current `main` at reconciliation start: `ca63790978521739fd81bba51a2227dc73785fcd`.
- The only open pull request is high-risk Nova hybrid-knowledge PR #1764. It remains unmerged and unapplied because it changes Supabase schema/function behavior and external-AI trust/data-flow boundaries.
- Nova Next deep-link/history parity issue #1783 remains open. No implementation was staged in this run because its own issue contract requires explicit design approval before code changes.
- No direct-to-main write or repository-protection bypass was used.

## Production-first triage

- Fresh read-only Guardian evidence shows 28 incidents total and 0 unresolved incidents.
- Guardian repair history contains terminal historical `failed`, `tests_failed`, `merged`, and `cancelled` rows; there is no unresolved incident requiring a new Guardian code-changing repair in this run.
- Fresh Supabase security advisor still reports the existing service-only/RLS-no-policy informational set, authenticated `SECURITY DEFINER` warnings previously reviewed, and leaked-password protection disabled. No Auth/RLS/security mutation was made because those are protected boundaries.
- Fresh Supabase performance advisor still reports 14 unindexed foreign keys, 8 RLS init-plan warnings and 54 unused-index informational findings. Issue #1770 already records why current zero-row workload evidence does not justify speculative protected schema changes.

## Backup / recovery finding

- `public.user_drive_backups` still contains 2 rows in `ready` state.
- Both ready rows have positive byte size, format version 1, AES-256-GCM metadata and 64-character ciphertext SHA-256 values.
- A non-destructive Google Drive read-back attempt using the exact Drive file ID recorded for each ready row returned Drive 404 / file not found from the currently connected operational Drive identity.
- This does not prove backup loss: the files may be owned by a different per-user OAuth/Drive identity than the current connector. It does prove database `ready` metadata alone is not sufficient evidence of recoverability from this operating context.
- Opened issue #1785 to trace the product credential/identity path and require supported read-back/hash verification before any recovery-readiness claim.
- No backup row, Drive file, credential, schema or production data was changed.

## Protected / blocked work

- PR #1764 remains blocked pending Beau's explicit approval at its exact head because it crosses production Supabase/schema and external-AI trust boundaries.
- Issue #1775 migration-history/pricing-authority reconciliation remains approval-gated; the old standalone central-pricing migration must not be applied merely to force history parity.
- Issue #1686 leaked-password protection remains approval-gated because it is an Auth configuration change.
- Production restore/overwrite, credential changes, auth/RLS, protected pricing authority, signing/release credentials and Guardian approval-policy changes remain human-approval gated.

## Next safe lanes

1. Trace backup upload/read-back ownership and credential routing without exposing backup contents or modifying production state.
2. Continue isolated Nova Next parity after the documented design gate is satisfied; do not promote over production Nova until parity evidence is complete.
3. Continue Admin web/mobile-web parity and freeze/usability audits using source and regression evidence before behavior changes.
4. Continue catalogue integrity and Australian-source enrichment without destructive merges of ambiguous records.
5. Re-check protected Supabase performance findings only after meaningful workload growth justifies a migration proposal.
