# Morley Ecosystem Autopilot — 2026-09-13 17:00 AWST

## Reconciled baseline
- Production `main` is `7645fd04d0e9b7b3d05d3d5ea681e079a3a000a4` after approved PR #1873 merged.
- Gumtree remains excluded.
- Recurring Nova knowledge maintenance remains fail-closed with zero active Nova maintenance cron jobs.

## Approved Nova scheduler diagnostics
- Human approval for protected PR #1873 was received before merge/deploy.
- Exact approved PR head `2d1329895970242090f09a8e5a18d282e35bf853` had green Nova Knowledge Maintenance Contract, Repository Security Audit, B&L Morley Quality Gate, Full Feature Contract Audit, Morley Restore Point Capture, Morley Email Contract and Morley Ultimate Parity Gate checks.
- PR #1873 squash-merged as `7645fd04d0e9b7b3d05d3d5ea681e079a3a000a4`.
- Production `nova-knowledge-maintenance` is ACTIVE version 4, `verify_jwt=false`, with the repository source for the approved bounded scheduler-auth diagnostics and the full repository versions of `knowledge_core.mjs` and `internal_adapters.mjs` deployed.
- Diagnostic authorization outcomes are bounded to `missing_config`, `missing_header`, `env_match`, `rpc_match`, `rpc_error` and `mismatch`; rejected requests still return only the generic 401 response.

## Controlled 4/4 runtime evidence
- `net._http_response` request 1834 hit the existing pg_net 5000 ms client timeout rather than returning an HTTP status. The corresponding worker-side maintenance run started and recorded a `Gateway Timeout` failure.
- Subsequent database-path requests 1835 and 1836 both returned HTTP 200 with `embedded_ready=4`, `embedded_error=0`, `ingested_skipped=4`, and durations of 3711 ms and 3113 ms respectively.
- Recent maintenance records likewise show successful completed 4/4 runs, confirming the reduced execution envelope remains viable when the request reaches an authorised worker and stays inside the HTTP timeout budget.
- The earlier intermittent HTTP 401 from request 1832 remains unresolved; successful repetitions alone are not sufficient evidence to restore recurring scheduling.

## Safety posture / unresolved work
- Nova recurring cron remains disabled until scheduled-path authorization is shown stable across repeated cycles and issue #1872 is resolved with evidence.
- PR #1859 remains blocked/stale because its 20-embedding schedule conflicts with production runtime evidence and must be reconciled with #1867/#1872/current main before any new protected approval decision.
- No credential value, Vault ownership, RLS, grant, matcher RPC, billing, destructive schema/data operation, or protected pricing policy was changed in this run.

## Next actions
1. Keep collecting bounded scheduled-path evidence at the verified 4/4 envelope without re-enabling recurring cron prematurely.
2. If another 401 occurs, use the approved diagnostic reason code to distinguish missing-header, environment match, RPC match/error or mismatch, without exposing credential material.
3. Reconcile or supersede #1859 only after #1872 has enough evidence to define a safe recurring schedule and timeout strategy.
4. Continue safe non-protected Admin/Morley Buys parity, login reliability and Nova Next work while the scheduler incident remains isolated.
