# Morley Ecosystem Autopilot — 2026-09-13 15:00 AWST

## Reconciled baseline
- Production `main` verified at `278d8fa988b126447fcdce9f332553adcc09e004` after PR #1867 merged.
- Gumtree remains excluded.
- Protected authentication/RLS, secrets/credentials, destructive production data, privileged roles/permissions, protected pricing authority, signing/release credentials, Guardian approval policy and repository/workflow security remain human-gated.

## Nova knowledge maintenance production recovery
- Root cause of issue #1866: the production service role lacks SELECT on optional operational feeds (`inventory_items`, `sales_records`, `valuation_quotes`), and the worker queried those feeds unconditionally.
- PR #1867 repaired the worker without broadening grants: optional permission-denied feeds are skipped, structured Supabase/PostgREST diagnostics are bounded/redacted, and required sources still fail closed.
- Exact PR head `f5a2d381915cbf05bd0628d7f4616ba0fac88b0f` passed Nova Knowledge Maintenance Contract, Repository Security Audit, B&L Morley Quality Gate, Full Feature Contract Audit, Morley Restore Point Capture, Morley Email Contract and Morley Ultimate Parity Gate before merge.
- Edge Function `nova-knowledge-maintenance` was deployed as production version 2 with existing custom scheduler authentication preserved (`verify_jwt=false`).
- Isolated bounded production invocation returned HTTP 200: 2 embeddings ready, 0 embedding errors, 4 internal knowledge documents created, 0 ingestion errors.
- Durable run `e99f9bb3-416c-48b2-9b25-2a58f8372a67` completed successfully with no error summary.
- Issue #1866 was closed after production verification.

## Resource-limit evidence
- A restored scheduled 20/12 batch returned HTTP 546 `WORKER_RESOURCE_LIMIT`; its orphaned `running` ledger row was reconciled to failed and the cron was unscheduled.
- Controlled manual probes at 4/4, 8/4, 12/4 and 12/12 all returned HTTP 200 with zero embedding errors; the 12/12 probe completed 12 embeddings in ~8.3 seconds.
- A subsequent real scheduled 12/12 cycle still returned HTTP 546, proving isolated/manual success does not establish safe cold/scheduled capacity. Its run `ea6521c8-d62c-4f47-9e41-551b49f3f118` was reconciled to failed and the cron was again unscheduled.
- Production scheduling is now conservatively restored as job 9 at `*/5 * * * *` with `embedding_limit=4, ingest_limit=4`, pending evidence from an actual scheduled cycle.
- Do not restore 12 or 20 scheduled embedding batches without fresh scheduled-run evidence. Source-controlled hardening must encode a production-safe bounded default rather than relying on manual probe capacity.

## Active protected lane
- PR #1859 hardens maintenance concurrency/claiming and shares ingestion logic between manual and scheduled knowledge paths.
- #1859 remains HIGH RISK / approval-gated because it adds a service-role-only RPC and Supabase migration. Do not merge/apply/deploy it without explicit human approval of its exact ready head.
- #1859 is also stale relative to current main and currently schedules 20 embeddings; it must preserve #1867 diagnostics/optional-source behavior and reconcile the verified scheduled resource limit before being considered ready.

## Production-first triage
- Guardian incidents: 28 resolved, no unresolved incident state observed in the fresh query.
- Backup metadata: 2 backups remain `ready`; latest backup creation is 2026-09-12 13:30 UTC. Recovery issue #1785 remains open because metadata alone is not read-back/restore evidence.
- Expected Nova Guard incident #1840 was closed as a successful fail-closed governance event after the explicitly approved #1830 path completed; the guard was not weakened.

## Other safe lanes awaiting reconciliation
- #1835 Nova Next offline static-cache PR is stale/conflicted (`mergeable=false`) and must be reconciled with current main before any merge.
- #1860 Morley Buys desktop functionality parity is open/stale and its Morley UI Consistency failures require diagnosis before any fresh merge attempt.
- #1841 Nova Next parity/UX completion remains draft/stale.
- Older documentation-only reconciliation PRs #1812 and #1843 are stale and should be closed once this ledger entry lands.

## Next actions
1. Observe a real job-9 scheduled 4/4 Nova maintenance cycle. If it succeeds, verify embedding health continues increasing; if it fails, unschedule again and treat scheduled embedding as requiring architectural batching changes.
2. Reconcile #1859 with current main and the scheduled-resource evidence, but keep protected migration/RPC deployment blocked pending explicit approval.
3. Diagnose the current Morley UI Consistency failure evidence before advancing desktop PR #1860.
4. Reconcile Nova Next offline work from fresh current main rather than merging stale/conflicted #1835.
5. Close stale run-ledger PRs after this reconciliation is merged.