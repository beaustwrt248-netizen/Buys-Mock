# Morley Autopilot Reconciliation — 2026-09-13 11:00 AWST

This is a non-sensitive append-only run note for the Morley ecosystem autopilot. Gumtree remains excluded unless Beau explicitly re-enables it.

## Repository / active work

- `main` remains `f40113ea01d59a437a8cc97bc40b9779f4cd10dd` from Nova Next functionality PR #1796.
- Documentation-only reconciliation PR #1812 remains open and mergeable. Its prior exact head completed Repository Security Audit, B&L Morley Quality Gate, Morley Email Contract and Morley Ultimate Parity Gate successfully; Nova PR Guard correctly skipped because the change was documentation-only.
- Protected Nova hybrid knowledge PR #1764, Nova Next CI/governance PR #1792 and Nova Next Pages deployment PR #1793 remain unmerged and approval-gated.

## Nova Next degraded-mode audit

- Opened issue #1813 after confirming `nova-next/service-worker.js` exists but Nova Next currently has no service-worker registration path.
- The service worker's static `CORE` precache is also stale relative to the current `app.js` module graph: current runtime imports include `live-runtime.mjs`, `feature-runtime.mjs`, `feature-ui.mjs`, `workspace-store.mjs`, `workspace-runtime.mjs`, `workspace-ui.mjs` and `file-session.mjs`, but those modules are not in the precache list.
- Result: current Nova Next cannot rely on that service worker for the requested safe degraded/offline shell behavior, and simply registering the existing worker would still leave offline module-resolution gaps.
- The bounded safe repair recorded in #1813 is to register the worker non-fatally, update/version the static runtime cache contract, keep authenticated/API responses out of precache, add regression coverage and verify an offline reload of a previously loaded isolated shell before claiming degraded-mode readiness.
- No production Nova route, deployment workflow, Supabase/Auth/RLS/schema, credential, pricing, Guardian, OTA/signing or production data was changed during this audit.

## Recovery / protected work

- Backup recovery issue #1785 remains open. Earlier operator-Drive 404s do not test the server-configured product OAuth identity used by the current backup function; canonical read-back/hash verification and an isolated restore/decrypt drill are still required before claiming recovery readiness.
- Issue #1775 migration-history/protected-pricing reconciliation and issue #1686 leaked-password protection remain approval-gated.
- Guardian code-changing repair PRs remain human-approval gated.

## Next safe lanes

1. Continue Nova Next isolated reliability/parity auditing, including #1813 degraded-mode coverage, without promoting it over production Nova.
2. Continue Admin web/mobile-web responsiveness, freezing and feature-parity auditing.
3. Continue read-only catalogue integrity and manufacturer-first Australian enrichment while protecting ambiguous production records and pricing-linked data.
4. Continue non-destructive backup/recovery verification preparation through the supported product credential path without changing credentials or production state.
