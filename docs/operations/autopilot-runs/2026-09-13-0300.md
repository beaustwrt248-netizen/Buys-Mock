# Morley Autopilot Reconciliation — 2026-09-13 03:00 AWST

This is a non-sensitive append-only run note for the canonical Morley autopilot state. Gumtree remains excluded.

## Repository / CI

- `main` remains `dc597dbc49ac739dd052f6751537817f64973a6f` (`#1776`, Nova Next bounded API timeout).
- Ledger PR `#1777` remains open and mergeable. Exact-head checks on `0769deb9be1e4cc45e271dcb5d4b480a22e3b93a` are green for Repository Security Audit, B&L Morley Quality Gate, Morley Email Contract, and Morley Ultimate Parity Gate; Nova PR Guard is skipped as non-applicable.
- Direct merge tooling did not complete the documentation-only merge in this run; no repository protection was bypassed.
- High-risk Nova/Supabase PR `#1764` remains unmerged and unapplied pending explicit approval.

## Production-first triage

- Connected Supabase project `ghdhairijqjqivqriigi` remains `ACTIVE_HEALTHY`.
- Guardian: all `guardian_incidents` are currently `resolved` (28 rows); no unresolved production incident is present. Historical repair rows include failed/cancelled/test-failed states, but no current incident requires autonomous repair.
- Nova telemetry: `nova_ai_runs` has no rows in the last seven days, so there is still insufficient production evidence for provider/default-routing changes.
- Backup metadata: `user_drive_backups` has 2 `ready` rows; the newest was created `2026-09-12 13:30:07+00`. In the last 14 days, backup events include 2 `backup_created` events and 1 `backup_verified` event. This is backup-freshness evidence only; full restore readiness still requires an isolated non-destructive read-back/restore drill.

## Security / performance evidence

- Supabase security advisor still reports leaked-password protection disabled; issue `#1686` remains approval-gated.
- Security advisor also reports 13 authenticated-callable `SECURITY DEFINER` RPCs. Read-only function inspection confirms Guardian control/decision RPCs and Admin invite/inventory RPCs perform explicit Admin/Manager authorization internally. `guardian_report_diagnostic` intentionally accepts authenticated callers and sanitizes sensitive-looking metadata keys. No authorization bypass was demonstrated.
- Performance advisor remains at 14 unindexed FKs, 8 RLS init-plan warnings, and 54 unused-index informational findings (`#1770`).
- All 14 currently flagged FK tables report approximately 0 live rows and very low historical sequential-scan counts (2–6). No leading covering index exists for the flagged FK columns, but current evidence does not show an urgent production bottleneck; speculative index changes remain inappropriate.
- The 8 flagged RLS policies are authenticated INSERT policies combining Morley role helpers with caller ownership (`created_by = auth.uid()` / `actor_user_id = auth.uid()`), plus restore-point relationship checks where applicable. Any future optimization must preserve authorization semantics exactly and remains protected.

## Catalogue integrity

- Manufacturer-first evidence resolves Apple hardware model `A2724` as Apple Watch SE (2nd generation), GPS + Cellular, 44mm, Europe/Asia-Pacific. The exact imported `MNQ23ZP/A` SKU-to-engraved-A-model mapping remains intentionally unasserted because secondary sources conflict.
- Apple `A3461` rows `1197` and `1202` remain the strongest likely identity-duplicate candidate. Row `1197`'s three audit-queue references (IDs 58, 205, 616) are all historical `verified` `missing_model_number` audits; none is pending/running/blocked. Row `1202` has no audit-queue row.
- Direct FK consumers of `device_catalog` are `device_buy_price_history`, `device_buy_prices`, `inventory_items`, `nova_catalog_audit_findings`, and `nova_catalog_audit_queue`. Prior checks found no pricing/history/inventory/finding references on either A3461 row. No merge, delete, deactivate, pricing, or protected data mutation was performed.
- Evidence was appended to issues `#1771` and `#1770`.

## Protected / blocked work

- `#1764` remains blocked pending explicit approval because it changes production Supabase schema/function behavior and external-AI data flow.
- `#1686` remains blocked pending explicit approval because it changes production Auth security configuration.
- Guardian code-changing repairs, destructive catalogue reconciliation, RLS/schema/index changes, production restore/overwrite, secrets/credentials, signing/OTA promotion, and protected pricing authority remain human-approval gated.

## Next safe lanes

1. Continue manufacturer-first catalogue verification and non-destructive dependency analysis for `#1771`.
2. Continue evidence-only performance/security analysis for `#1770`; do not mutate schema/RLS/indexes.
3. Continue Nova Next parity/accessibility/evaluation audits without promoting it over production Nova or wiring protected production auth.
4. Reconcile backup integrity and restore-readiness evidence without production overwrite.
