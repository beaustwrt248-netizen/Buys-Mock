# Morley Autopilot Reconciliation — 2026-09-13 10:00 AWST

This is a non-sensitive append-only run note for the Morley ecosystem autopilot. Gumtree remains excluded unless Beau explicitly re-enables it.

## Repository / active work

- Material `main` advanced to `f40113ea01d59a437a8cc97bc40b9779f4cd10dd` through PR #1796.
- PR #1796 adds isolated Nova Next functionality for Tasks, Projects, Calendar, Files, Automation status and Integrations status, plus Settings navigation into the live status surfaces.
- Current production `nova/**`, production Supabase schema/RLS, protected pricing authority, Guardian repair authority, release/OTA/signing and deployment routing were not changed by #1796.
- The temporary branch-only Nova Next validator workflow used during implementation was removed before merge. Permanent CI/governance PR #1792 remains separate and explicit-approval gated.
- Full isolated validation immediately before cleanup passed 70/70 Nova Next Node tests, JavaScript syntax checks, isolated Android package-identity checks, Gradle unit/lint/debug-APK assembly and APK hash/artifact staging.
- Final exact head `908716c31834924f0da0a397b0a994d12f2aa11e` was green for Repository Security Audit, B&L Morley Quality Gate, Morley Email Contract, Nova PR Guard and Morley Ultimate Parity Gate before squash merge.

## Backup / recovery reconciliation

- Issue #1785 remains open. Read-only source tracing confirms the current `google-drive-backup` function uses the server-configured Google user-OAuth refresh identity to upload backups and immediately read the uploaded file back, parse it and verify SHA-256 before reporting success.
- Therefore, earlier `file not found` results obtained through the separately connected operator Drive identity do not test the canonical credential path used by the backup function. They neither prove backup loss nor prove end-to-end recovery readiness.
- The next safe recovery check is supported read-back/hash verification through the configured product OAuth identity, followed by an isolated non-production decrypt/restore drill. Credential replacement and production overwrite remain protected.

## Protected / blocked work

- PR #1764 remains explicit-approval gated because it changes production Supabase schema/function behavior and external-AI trust/data-flow boundaries.
- PR #1792 remains explicit-approval gated because it changes GitHub CI/governance workflow behavior.
- PR #1793 remains explicit-approval gated because it adds Nova Next Pages deployment/publishing behavior.
- Issue #1775 migration-history/protected-pricing reconciliation remains approval-gated.
- Issue #1686 leaked-password protection remains approval-gated as an Auth configuration change.
- Guardian code-changing repairs remain human-approval gated.

## Next safe lanes

1. Continue isolated Nova Next capability/parity work while keeping production auth, schema, deployment, package/signing and promotion separate.
2. Continue Admin web/mobile-web reliability, responsiveness and parity auditing with regression-first fixes.
3. Continue manufacturer-first catalogue integrity and Australian-source enrichment without guessing or destructive ambiguity resolution.
4. Continue backup implementation tracing and non-destructive supported-identity verification preparation without changing credentials or production data.
