# Source-test ownership

`DriveRecoveryTimestampSourceContractTest` lives with the native Morley Android unit tests. It directly exercises the production formatter instead of duplicating timestamp conversion logic in test-only code.
