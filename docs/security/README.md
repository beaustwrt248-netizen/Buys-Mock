# Security hardening notes

- `android-pr-permissions-split.md` — staged migration of Android pull-request validation from the write-capable production build workflow to a dedicated read-only workflow.
