# Nova Next Functionality Execution Note

This branch temporarily includes `.github/workflows/nova-next-ci.yml` solely so draft-PR CI can run the complete Nova Next contract suite and Android debug build while the permanent validator remains protected in PR #1792.

The workflow file must not be treated as a duplicate governance change for final merge. Before this feature branch is considered merge-ready, reconcile it against PR #1792/main so only one permanent validator workflow remains.
