# Morley 2.15.79 OTA intent

After protected checks pass and this PR merges to `main`, the existing Build B&L Morley APK and Auto OTA Release workflows are expected to build the signed 2.15.79 APK and promote `ota/latest.json` through the repository's protected OTA flow.
