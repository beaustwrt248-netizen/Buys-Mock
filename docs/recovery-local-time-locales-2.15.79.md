# Locale behaviour

The formatter follows the Android device locale rather than hard-coding an Australian text pattern. Australian devices therefore receive familiar local presentation, while other authorized users receive an equivalent date/time format appropriate to their device locale.
