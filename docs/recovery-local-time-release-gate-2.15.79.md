# Release gate

Do not manually bypass repository rules for this release. Merge only through the protected PR path after required checks pass, then use the existing production signing and OTA workflows so the published APK remains signed by the established Morley release key.
