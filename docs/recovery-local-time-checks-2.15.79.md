# Protected checks for Morley 2.15.79

Required repository rules include the `audit` and `security-audit` GitHub Actions checks. The normal PR suite also runs Android validation, APK build, quality, UI consistency, OTA policy, feature-contract and parity workflows. Merge must remain blocked until the repository rules permit it.
