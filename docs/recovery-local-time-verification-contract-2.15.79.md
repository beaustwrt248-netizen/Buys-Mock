# Verification contract unchanged

Backup verification still operates on the encrypted backup identified by backend backup ID. Formatting the display copy of `created_at` cannot alter cryptographic verification, hashes, Drive file identity or backup ownership checks.
