# Existing backups

No re-backup is required to obtain friendly timestamps. Existing history entries already contain absolute `created_at` metadata; 2.15.79 formats those values when they are returned by the existing list/health actions.
