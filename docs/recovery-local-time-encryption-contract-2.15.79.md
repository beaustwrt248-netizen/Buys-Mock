# Encryption contract unchanged

AES-256-GCM encryption and server-side key wrapping are untouched. No key material is added to the Android formatter or UI, and no encrypted payload is decrypted or transformed by the local timestamp helper.
