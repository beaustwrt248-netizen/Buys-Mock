# Recovery local-time review summary

The patch is deliberately narrow: version bump, timestamp formatter, application of the formatter to backup snapshot display values, and regression coverage. It does not change Recovery Centre Compose structure or any authenticated backup operation.
