(()=>{
const $=(s,r=document)=>r.querySelector(s),H='buysmock_valuation_history_v1',W='morley_watchlist_v1';
const read=(k,f)=>{try{return JSON.parse(localStorage.getItem(k)||JSON.stringify(f))}catch{return f}};
const money=n=>new Intl.NumberFormat('en-AU',{style:'currency',currency:'AUD',maximumFractionDigits:0}).format(Number(n)||0);
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const key=x=>`${x.type}|${x.query}`;
function verdict(x){const ask=Number(x.ask)||0,max=Number(x.maxBuy)||0,market=Number(x.usedValue)||0;if(!ask)return 'REVIEW';if(max&&ask<=max)return 'GREAT BUY';if(market&&ask<=market*.78)return 'GOOD BUY';if(market&&ask<market)return 'MARGINAL';return 'AVOID'}
function profit(x){const ask=Number(x.ask)||0,market=Number(x.usedValue)||0;return ask&&market?Math.max(0,market-ask):0}
function savedAt(x){const raw=x?.savedAt||x?.createdAt||x?.timestamp||x?.date||x?.time;const d=raw?new Date(raw):null;return d&&!Number.isNaN(d.getTime())?d:null}
function relativeTime(d){if(!d)return 'No saved activity yet';const mins=Math.max(0,Math.round((Date.now()-d.getTime())/60000));if(mins<1)return 'Saved just now';if(mins<60)return `Saved ${mins}m ago`;const hrs=Math.round(mins/60);if(hrs<24)return `Saved ${hrs}h ago`;const days=Math.round(hrs/24);return `Saved ${days}d ago`}
function openItem(x){if(!x)return;if(x.type==='laptop'){window.show?.('laptop');if($('#lapModel'))$('#lapModel').value=x.query||'';if($('#lapAsk'))$('#lapAsk').value=x.ask||'';setTimeout(()=>$('#lapAnalyse')?.click(),80)}else if(x.type==='deal'){window.show?.('deals')}else{window.show?.('desktop');if($('#deskSpecs'))$('#deskSpecs').value=x.query||'';if($('#deskAsk'))$('#deskAsk').value=x.ask||'';setTimeout(()=>$('#deskAnalyse')?.click(),80)}}
function style(){if($('#morleySmartWorkspacePlusStyle'))return;const s=document.createElement('style');s.id='morleySmartWorkspacePlusStyle';s.textContent=`.mswp-panel{margin-top:10px;background:linear-gradient(145deg,rgba(9,27,51,.96),rgba(6,17,34,.98));border:1px solid rgba(22,199,255,.18);border-radius:18px;padding:14px}.mswp-head{display:flex;justify-content:space-between;gap:10px;align-items:center}.mswp-head h3{margin:0;font-size:16px}.mswp-status{display:flex;align-items:center;gap:7px;color:#8ea6c4;font-size:10px;font-weight:900}.mswp-dot{width:7px;height:7px;border-radius:50%;background:#57e389;box-shadow:0 0 9px rgba(87,227,137,.75)}.mswp-status.offline .mswp-dot{background:#ffc857;box-shadow:0 0 9px rgba(255,200,87,.55)}.mswp-activity{margin-top:8px;color:#8ea6c4;font-size:10px}.mswp-list{margin-top:8px}.mswp-row{display:flex;align-items:center;gap:10px;padding:10px 0;border-top:1px solid rgba(255,255,255,.06)}.mswp-row:first-child{border-top:0}.mswp-copy{min-width:0;flex:1}.mswp-copy b,.mswp-copy small{display:block;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.mswp-copy small{color:#8ea6c4;margin-top:3px}.mswp-open,.mswp-remove{border:1px solid rgba(22,199,255,.28);background:#0a1b33;color:#fff;border-radius:10px;padding:7px 9px;font-weight:900;cursor:pointer}.mswp-remove{color:#ff9aa8}.mswp-profit{font-size:10px;font-weight:900;color:#57e389}.mswp-empty{color:#8ea6c4;font-size:12px;margin-top:8px;display:block}.mswp-insight{display:flex;justify-content:space-between;gap:10px;align-items:center;margin-top:8px;padding:10px;border-radius:12px;background:rgba(22,199,255,.05)}.mswp-insight small{color:#8ea6c4}.mswp-insight b{font-size:13px}@media(max-width:620px){.mswp-row{align-items:flex-start;flex-wrap:wrap}.mswp-open,.mswp-remove{flex:1}.mswp-head{align-items:flex-start}}`;document.head.appendChild(s)}
function render(){const root=$('#morleySmartWorkspace');if(!root)return;style();let panel=$('#morleySmartWorkspacePlus');if(!panel){panel=document.createElement('div');panel.id='morleySmartWorkspacePlus';panel.className='mswp-panel';root.appendChild(panel)}const history=read(H,[]),watched=read(W,[]);const items=watched.map(k=>history.find(x=>key(x)===k)).filter(Boolean).slice(0,8);const active=history.filter(x=>['GREAT BUY','GOOD BUY'].includes(verdict(x)));const potential=active.reduce((sum,x)=>sum+profit(x),0);const newest=history.map(savedAt).filter(Boolean).sort((a,b)=>b-a)[0]||null;const online=navigator.onLine!==false;panel.innerHTML=`<div class="mswp-head"><h3>★ Watchlist</h3><span class="mswp-status ${online?'':'offline'}"><span class="mswp-dot"></span>${online?'LIVE':'OFFLINE · CACHED'}</span></div><div class="mswp-activity">${esc(relativeTime(newest))} · ${history.length} valuation${history.length===1?'':'s'} available locally</div><div class="mswp-insight"><div><small>Potential margin across current buy opportunities</small><b>${money(potential)}</b></div><small>${active.length} opportunity${active.length===1?'':'ies'}</small></div><div class="mswp-list">${items.length?items.map((x,i)=>`<div class="mswp-row"><div class="mswp-copy"><b>${x.type==='laptop'?'💻':x.type==='deal'?'⚡':'🖥️'} ${esc(x.query||'Untitled valuation')}</b><small>${money(x.usedValue)} market · ${money(x.maxBuy)} max buy${x.ask?` · ${money(x.ask)} ask`:''}</small>${profit(x)?`<span class="mswp-profit">${money(profit(x))} potential gross margin</span>`:''}</div><button class="mswp-open" data-open="${i}">Open</button><button class="mswp-remove" data-remove="${i}" aria-label="Remove from watchlist">Remove</button></div>`).join(''):'<span class="mswp-empty">Star a recent valuation to keep it here for fast re-checking.</span>'}</div>`;panel.querySelectorAll('[data-open]').forEach(b=>b.onclick=()=>openItem(items[Number(b.dataset.open)]));panel.querySelectorAll('[data-remove]').forEach(b=>b.onclick=()=>{const x=items[Number(b.dataset.remove)];if(!x)return;localStorage.setItem(W,JSON.stringify(read(W,[]).filter(v=>v!==key(x))));window.dispatchEvent(new Event('morley-watchlist-updated'));render()})}
function boot(){render();window.addEventListener('storage',render);window.addEventListener('morley-watchlist-updated',render);window.addEventListener('online',render);window.addEventListener('offline',render);document.addEventListener('click',e=>{if(e.target?.matches?.('[data-watch]'))setTimeout(render,50)});new MutationObserver(()=>{if($('#morleySmartWorkspace')&&!$('#morleySmartWorkspacePlus'))render()}).observe(document.body,{childList:true,subtree:true})}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(boot,450));else setTimeout(boot,450);
})();