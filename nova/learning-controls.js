(()=>{'use strict';
const SB='https://ghdhairijqjqivqriigi.supabase.co',KEY='sb_publishable_ch49o8WRnDb8pPzowZH3Tg_XZcIbgvt';const $=s=>document.querySelector(s),esc=s=>String(s??'').replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[m]));
async function invoke(body){const token=window.NovaAuth?.getAccessToken?.();if(!token)throw new Error('Nova authentication is required.');const r=await fetch(`${SB}/functions/v1/nova-learning`,{method:'POST',headers:{apikey:KEY,Authorization:`Bearer ${token}`,'Content-Type':'application/json'},body:JSON.stringify(body),cache:'no-store'});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||`Nova learning HTTP ${r.status}`);return d}
function install(){const page=$('[data-page="knowledge"]');if(!page||$('#novaLearningControls'))return;const card=document.createElement('article');card.className='card';card.id='novaLearningControls';card.innerHTML=`<div class="section-head"><div><div class="eyebrow">EXPLICIT OUTCOME LEARNING</div><h2>Learning Controls</h2></div><span id="learningControlState" class="tag">READY</span></div><p>Nova can refresh learned patterns from already-recorded verified Guardian, valuation, inventory and sales outcomes. This never runs silently. Human feedback is stored as unverified context until corroborated by source data.</p><button id="learningHarvest" type="button">Learn from verified outcomes</button><div style="margin-top:16px"><label for="learningDomain">Feedback domain</label><select id="learningDomain" style="margin-left:8px"><option value="catalogue">Catalogue</option><option value="pricing">Pricing</option><option value="guardian">Guardian</option><option value="support">Support</option><option value="release">Release</option><option value="valuation">Valuation</option><option value="inventory">Inventory</option><option value="sales">Sales</option><option value="admin">Admin</option></select><textarea id="learningFeedback" rows="4" placeholder="What should Nova learn from this approved/rejected outcome?" style="box-sizing:border-box;width:100%;margin-top:8px;border:1px solid #1d3c31;background:#07110e;color:#e8fff6;border-radius:12px;padding:12px"></textarea><button id="learningFeedbackSave" class="secondary" type="button" style="margin-top:8px">Save explicit feedback</button></div><div id="learningControlMessage" class="callout">No learning write occurs until you press one of these buttons.</div>`;page.appendChild(card);$('#learningHarvest').addEventListener('click',harvest);$('#learningFeedbackSave').addEventListener('click',feedback)}
async function harvest(){const b=$('#learningHarvest'),m=$('#learningControlMessage'),state=$('#learningControlState');b.disabled=true;state.textContent='LEARNING';try{const d=await invoke({action:'harvest'});m.textContent=`Outcome learning refreshed: ${Number(d.written||0)} experience upserts from ${Number(d.incident_sample||0)} incidents, ${Number(d.repair_sample||0)} repairs, ${Number(d.valuation_sample||0)} valuations, ${Number(d.inventory_sample||0)} inventory records and ${Number(d.sales_sample||0)} sales records. Existing protected controls were not changed.`;state.textContent='UPDATED';state.className='tag ok';await window.NovaMemoryLive?.refresh?.()}catch(error){console.error(error);m.textContent=error.message||String(error);state.textContent='FAILED';state.className='tag high'}finally{b.disabled=false}}
async function feedback(){const domain=$('#learningDomain')?.value||'',summary=String($('#learningFeedback')?.value||'').trim(),b=$('#learningFeedbackSave'),m=$('#learningControlMessage'),state=$('#learningControlState');if(!summary)return;m.textContent='Saving explicit unverified feedback…';b.disabled=true;try{await invoke({action:'feedback',domain,summary});$('#learningFeedback').value='';m.textContent='Feedback saved as unverified human context (confidence 0.6). It does not silently override current evidence or protected rules.';state.textContent='FEEDBACK';state.className='tag ok';await window.NovaMemoryLive?.refresh?.()}catch(error){console.error(error);m.textContent=error.message||String(error);state.textContent='FAILED';state.className='tag high'}finally{b.disabled=false}}
window.NovaLearningControls={invoke};if(document.readyState==='loading')addEventListener('DOMContentLoaded',install,{once:true});else install();addEventListener('nova:authenticated',install);
})();
