(()=>{'use strict';
const $=s=>document.querySelector(s);const money=n=>new Intl.NumberFormat('en-AU',{style:'currency',currency:'AUD',maximumFractionDigits:2}).format(Number(n||0));
function install(){const page=$('[data-page="business"]');if(!page||$('#novaScenarioPlanner'))return;const card=document.createElement('article');card.className='card';card.id='novaScenarioPlanner';card.innerHTML=`<div class="section-head"><div><div class="eyebrow">WHAT-IF ANALYSIS</div><h2>Scenario Planner</h2></div><span class="tag">ADVISORY</span></div><p>Model a pricing or discount change without touching protected pricing. Enter expected sale price, acquisition cost, fees/other costs and expected unit volume.</p><div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(150px,1fr));gap:10px"><label>Current sale price<input id="scenarioCurrent" type="number" min="0" step="0.01" value="500"></label><label>Proposed sale price<input id="scenarioProposed" type="number" min="0" step="0.01" value="450"></label><label>Acquisition cost<input id="scenarioCost" type="number" min="0" step="0.01" value="250"></label><label>Fees / other per unit<input id="scenarioFees" type="number" min="0" step="0.01" value="30"></label><label>Expected current units<input id="scenarioUnitsCurrent" type="number" min="0" step="1" value="10"></label><label>Expected proposed units<input id="scenarioUnitsProposed" type="number" min="0" step="1" value="13"></label></div><button id="scenarioRun" type="button" style="margin-top:12px">Compare scenario</button><div id="scenarioOutput" class="stack" style="margin-top:12px"></div><div class="callout">Scenario output is not a pricing approval and never changes catalogue or protected buy/sell prices.</div>`;page.appendChild(card);$('#scenarioRun').addEventListener('click',run);run()}
function run(){const v=id=>Math.max(0,Number($(`#${id}`)?.value||0)),cur=v('scenarioCurrent'),next=v('scenarioProposed'),cost=v('scenarioCost'),fees=v('scenarioFees'),u1=v('scenarioUnitsCurrent'),u2=v('scenarioUnitsProposed'),gp1=cur-cost-fees,gp2=next-cost-fees,total1=gp1*u1,total2=gp2*u2,rev1=cur*u1,rev2=next*u2,discount=cur>0?(cur-next)/cur:0,delta=total2-total1,margin1=cur>0?gp1/cur:0,margin2=next>0?gp2/next:0;const out=$('#scenarioOutput');if(!out)return;const row=(a,b,c='')=>`<div class="item"><div><b>${a}</b><small>${b}</small></div>${c?`<span class="tag ${c==='NEGATIVE'?'high':'ok'}">${c}</span>`:''}</div>`;out.innerHTML=[row('Current scenario',`${u1} units • ${money(rev1)} revenue • ${money(total1)} total gross profit • ${(margin1*100).toFixed(1)}% unit margin`),row('Proposed scenario',`${u2} units • ${money(rev2)} revenue • ${money(total2)} total gross profit • ${(margin2*100).toFixed(1)}% unit margin`),row('Price change',`${(discount*100).toFixed(1)}% discount • volume change ${u2-u1>=0?'+':''}${u2-u1} units`),row('Projected profit change',`${delta>=0?'+':''}${money(delta)} versus current scenario`,delta>=0?'POSITIVE':'NEGATIVE')].join('')}
window.NovaScenarioPlanner={run};if(document.readyState==='loading')addEventListener('DOMContentLoaded',install,{once:true});else install();addEventListener('nova:authenticated',install);new MutationObserver(()=>install()).observe(document.documentElement,{childList:true,subtree:true});
})();
