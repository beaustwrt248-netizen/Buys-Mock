(()=>{'use strict';
const $=s=>document.querySelector(s);let recognition=null,listening=false;
function ctor(){return window.SpeechRecognition||window.webkitSpeechRecognition||null}
function install(){const card=$('#novaConversation');if(!card||$('#novaVoiceBtn'))return;const compose=card.querySelector('.nova-conversation-compose');if(!compose)return;const button=document.createElement('button');button.id='novaVoiceBtn';button.type='button';button.className='secondary';button.textContent='🎙 Voice';button.setAttribute('aria-label','Talk to Nova');compose.appendChild(button);const status=document.createElement('div');status.id='novaVoiceStatus';status.className='nova-conversation-hint';status.textContent=ctor()?'Voice ready · speech stays in the browser speech service supported by this device.':'Voice recognition is not supported by this browser.';compose.insertAdjacentElement('afterend',status);button.disabled=!ctor();button.addEventListener('click',toggle)}
function toggle(){if(listening){recognition?.stop();return}const C=ctor();if(!C)return;recognition=new C();recognition.lang='en-AU';recognition.interimResults=false;recognition.continuous=false;recognition.maxAlternatives=1;recognition.onstart=()=>{listening=true;$('#novaVoiceBtn').textContent='■ Stop';$('#novaVoiceStatus').textContent='Listening… Ask Nova naturally.'};recognition.onresult=e=>{const text=String(e.results?.[0]?.[0]?.transcript||'').trim();const input=$('#novaConversationInput');if(input&&text){input.value=text;input.dispatchEvent(new KeyboardEvent('keydown',{key:'Enter',bubbles:true}))}};recognition.onerror=e=>{$('#novaVoiceStatus').textContent=`Voice unavailable: ${e.error||'recognition error'}.`};recognition.onend=()=>{listening=false;const b=$('#novaVoiceBtn');if(b)b.textContent='🎙 Voice';const s=$('#novaVoiceStatus');if(s&&!/unavailable/i.test(s.textContent||''))s.textContent='Voice ready.'};recognition.start()}
function watch(){install();new MutationObserver(()=>install()).observe(document.body,{childList:true,subtree:true})}
if(document.readyState==='loading')addEventListener('DOMContentLoaded',watch,{once:true});else watch();addEventListener('nova:authenticated',install);
})();
